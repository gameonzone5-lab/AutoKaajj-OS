name: Build AutoKaaj OS APK

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Source Code
      uses: actions/checkout@v4

    - name: Set up JDK 11
      uses: actions/setup-java@v4
      with:
        java-version: '11'
        distribution: 'temurin'

    - name: Fix NDK Missing Platforms (Magic Trick)
      run: |
        export NDK_DIR=/usr/local/lib/android/sdk/ndk/25.2.9519653
        echo "ndk.dir=$NDK_DIR" > local.properties
        mkdir -p $NDK_DIR/platforms

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build Debug APK with Gradle
      run: ./gradlew assembleDebug

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: AutoKaaj-OS-App
        path: app/build/outputs/apk/debug/*.apk
